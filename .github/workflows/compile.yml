name: build

on: [push]

jobs:
    build:
        runs-on: ubuntu-22.04
        steps:
            - uses: actions/checkout@v1
            - name: Compile Nginx
              run: sudo bash -ex ${GITHUB_WORKSPACE}/build.sh
