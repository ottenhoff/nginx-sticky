name: build

on: [push]

jobs:
    build:
        runs-on: ubuntu-22.04
        steps:
            - uses: actions/checkout@v1
            - name: Compile Nginx
              run: bash ${GITHUB_WORKSPACE}/build.sh
